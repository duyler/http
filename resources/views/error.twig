<!doctype html>
<html lang="en" class="h-100" data-bs-theme="auto">
    <head>

        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="description" content="">
        <title>{{ status }} {{ message }}</title>

        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

    </head>
    <body class="d-flex h-100">

        <div class="container d-flex w-100 h-100 p-3 flex-column">

            <div class="message">
                <h1>{{ status }} {{ message }}</h1>
            </div>

            <div class="trace">
                <pre><code>{{ trace }}</code></pre>
            </div>

            <p><strong>Error action: </strong>{{ errorAction }}</p>
            <p><strong>Current action: </strong>{{ beginAction }}</p>

            <div class="accordion" id="accordionPanelsStayOpenExample">
                <div class="accordion-item">
                    <h2 class="accordion-header">
                        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#panelsStayOpen-collapseAction" aria-expanded="false" aria-controls="panelsStayOpen-collapseAction">
                            Complete log
                        </button>
                    </h2>
                    <div id="panelsStayOpen-collapseAction" class="accordion-collapse collapse">
                        <div class="accordion-body">
                            <ul>
                                {% for action in actionLog %}
                                    <li>{{ action }}</li>
                                {% endfor %}
                            </ul>
                        </div>
                    </div>
                </div>
                <div class="accordion-item">
                    <h2 class="accordion-header">
                        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#panelsStayOpen-collapseSuccess" aria-expanded="false" aria-controls="panelsStayOpen-collapseSuccess">
                            Success log
                        </button>
                    </h2>
                    <div id="panelsStayOpen-collapseSuccess" class="accordion-collapse collapse">
                        <div class="accordion-body">
                            <ul>
                                {% for action in successLog %}
                                    <li>{{ action }}</li>
                                {% endfor %}
                            </ul>
                        </div>
                    </div>
                </div>

                <div class="accordion-item">
                    <h2 class="accordion-header">
                        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#panelsStayOpen-collapseFail" aria-expanded="false" aria-controls="panelsStayOpen-collapseFail">
                            Fail log
                        </button>
                    </h2>
                    <div id="panelsStayOpen-collapseFail" class="accordion-collapse collapse">
                        <div class="accordion-body">
                            <ul>
                                {% for action in failLog %}
                                    <li>{{ action }}</li>
                                {% endfor %}
                            </ul>
                        </div>
                    </div>
                </div>

                <div class="accordion-item">
                    <h2 class="accordion-header">
                        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#panelsStayOpen-collapseRetries" aria-expanded="false" aria-controls="panelsStayOpen-collapseRetries">
                            Retries log
                        </button>
                    </h2>
                    <div id="panelsStayOpen-collapseRetries" class="accordion-collapse collapse">
                        <div class="accordion-body">
                            <ul>
                                {% for action in retriesLog %}
                                    <li>{{ action }}</li>
                                {% endfor %}
                            </ul>
                        </div>
                    </div>
                </div>

                <div class="accordion-item">
                    <h2 class="accordion-header">
                        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#panelsStayOpen-collapseSuspended" aria-expanded="false" aria-controls="panelsStayOpen-collapseSuspended">
                            Suspended actions
                        </button>
                    </h2>
                    <div id="panelsStayOpen-collapseSuspended" class="accordion-collapse collapse">
                        <div class="accordion-body">
                            <ul>
                                {% for action in suspendedLog %}
                                    <li>{{ action }}</li>
                                {% endfor %}
                            </ul>
                        </div>
                    </div>
                </div>
            </div>

            <footer class="mt-auto">
                <p>&copy; Duyler {{ 'now' | date('Y') }}</p>
            </footer>
        </div>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
    </body>
</html>
